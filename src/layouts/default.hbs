<!DOCTYPE html>
<html lang="en"{{#if page.component}} class="component-{{page.component.name}}{{#if (is-cloud-theme page)}} theme-cloud{{/if}}"{{/if}}>
  <head>
{{> head defaultPageTitle='Untitled'}}
  </head>
  <body class="article{{#if page.component}} component-{{page.component.name}}{{#if (is-cloud-theme page)}} theme-cloud{{/if}}{{/if}}{{#with (or page.attributes.role page.role)}} {{{this}}}{{/with}}">
{{> header}}
{{> body}}
{{> footer}}
  </body>
</html>
